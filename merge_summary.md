此次合并主要包含两个变更：删除了未使用的 HTTP 包导入，并添加了编译后的后端服务器二进制文件。这些变更旨在优化代码结构并提供可执行的服务器文件。
| 文件 | 变更 |
|------|---------|
| backend/internal/handler/exec.go | - 删除了未使用的 net/http 导入，优化了代码结构 |
| backend/k8s-dashboard-server | - 新增了编译后的后端服务器二进制文件，提供可执行的服务器程序 |